# V4 Train — T06 → T07 → T08 — 2026-09-24

Status: **PREPARED / NOT YET LAUNCHED**

## 1. Base and purpose

```text
BASE_BRANCH=3.0-main
BASE_SHA=f58b7823e7050a6387f59e5bd27007398a7bd41c
TICKETS=T06 #10 → T07 #11 → T08 #12
MAX_CONCURRENCY=1
```

Purpose: complete the core V4 user path from document ingestion through domain-backed review to separated Safe Output / Confidential Audit, without mixing in the later recognizer/operator migration.

## 2. Why this composition

- T06 and T07 are currently unblocked; map order chooses T06 first.
- T08 becomes executable only after T07 is closed/accepted.
- The three tickets form one coherent integration path: source → Job → ReviewSession → Privacy Gate → output.
- T11+ changes engine policy architecture and is deliberately outside this train.
- T17/T18 are parallel batch/structured branches and are deliberately outside this train.

Fresh frontier discovery remains mandatory before selecting each next ticket.

## 3. Runtime authority

Use the current Atenea native production path from Atenea `docs/START_HERE.md`.

At preparation time the verified machine state is:

```text
Pi=0.87.1
Gentle AI=3.7.0
active profile=native-balanced
Atenea conformance checks=PASS
```

These values are evidence at preparation time, not permanent project configuration. Re-run preflight before launch and do not pin reviewer models in this repository.

Start a **fresh Pi session** from a clean isolated worktree. Do not resume the PR #35 Pi session or its hidden state.

## 4. Execution ownership

Give Pi/Gentle semantic authority, not orchestration prose.

Native Gentle owns:
- ODD/exploration;
- decomposition/workers;
- verification;
- work-unit commits;
- RDD/risk/review timing;
- correction lifecycle;
- acknowledgement/burn.

Project authority owns WHAT/WHY/acceptance. The runtime may decompose HOW but may not expand scope.

## 5. Ticket sequence

### T06 / #10 — Unified text/document input adapters
Execute first if preflight remains green. Treat `.doc`, scanned/no-text PDF, extraction failure, memory-only handling and parser dependency security as explicit acceptance surfaces.

### T07 / #11 — Review workspace wired to ReviewSession
Rediscover GitHub frontier after T06. UI actions must mutate domain state, not DOM state; responsive/accessibility behavior and manual/restore/modify paths require deterministic evidence.

### T08 / #12 — Safe Output and Confidential Audit split
Execute only after T07 is accepted/closed per blocker authority. Safe export must be fail-closed on pending review and structurally incapable of including confidential mapping/notes.

## 6. Train closeout before publication

After T08, STOP ticket execution and run `QUALITY_EXECUTION_PROTOCOL_V1` composed-state closeout.

Minimum train-specific integrated scenarios:
- synthetic TXT → Job → ReviewSession → decision → Privacy Gate → Safe Output;
- synthetic DOCX → same path;
- synthetic text-bearing PDF → same path with the PDF.js eval guard still green;
- no-text/scanned PDF → explicit non-success and no safe export;
- `.doc` → explicit unsupported result, never silently accepted;
- modify / restore / manual review actions affect final domain output correctly;
- pending mandatory decision blocks Safe Output;
- Confidential Audit remains a separate artifact/surface and Safe Output contains no trace mapping or reviewer notes;
- existing no-network/checker self-tests remain green;
- debt entries owned by T06/T07/T08 are reconciled.

Also run the full repository test/build/type/lint/format chain and prove a clean tree after generation/build.

## 7. Publication boundary

Default launch authority for this prepared train is local implementation only:

```text
NO PUSH
NO PR
NO ISSUE CLOSE/EDIT DURING EXECUTION
NO MERGE
NO FORCE PUSH
NO HISTORY REWRITE
```

Human/Cora may separately authorize publication after deterministic closeout. Final merge remains human-owned.

## 8. STOP conditions

STOP on:
- material conflict among current product authorities;
- a ticket whose blocker is still open at selection time;
- native Gentle ambiguous/no-safe continuation;
- unresolved parser/security decision that changes product scope;
- required work that would materially expand beyond T06/T07/T08;
- failed deterministic closeout that cannot be repaired as a bounded correction.
